box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "HEAD"

deploy:
  steps:
    - simpletechs/ftp-deploy-exclude:
        destination: ftp://ws44.surftown.com/kimbrerfesten.dk/hugo
        username: kimbreradmin
        password: $FTP_PASS
        remote-file: ./protected/werckerSync.txt
        exclude-file: "/.git/ /.gitignore /wercker.yml /content/ config.toml"