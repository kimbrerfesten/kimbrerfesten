box: golang:latest
build:
  steps:
    - arjen/hugo-build:
        version: "0.19"
        theme: hugo-kimbrer
        flags: --buildDrafts=false

deploy:
  steps:
    - duleorlovic/ftp-deploy:
        destination: ftp://ws44.surftown.com/kimbrerfesten.dk/
        username: kimbreradmin
        password: $FTP_PASS
        remote-file: ./protected/werckerSync.txt
        subfolder: public
